'use client';

import { ReactNode } from 'react';

export default function WorksheetsLayout({
  children,
}: {
  children: ReactNode;
}) {
  return (
    <>
      {children}
    </>
  );
}